package com.myprojects.qrscanner.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Color palette for the QR Scanner application.
 * 
 * This file defines the color constants used throughout the application.
 * The colors are designed to work well in both light and dark themes,
 * providing good contrast and accessibility.
 * 
 * @author Edouard Makon
 * @since 1.0
 */

/**
 * Purple color for dark theme (80% opacity).
 * Used as the primary color in dark mode.
 */
val Purple80 = Color(0xFFD0BCFF)

/**
 * Purple-grey color for dark theme (80% opacity).
 * Used as the secondary color in dark mode.
 */
val PurpleGrey80 = Color(0xFFCCC2DC)

/**
 * Pink color for dark theme (80% opacity).
 * Used as the tertiary color in dark mode.
 */
val Pink80 = Color(0xFFEFB8C8)

/**
 * Purple color for light theme (40% opacity).
 * Used as the primary color in light mode.
 */
val Purple40 = Color(0xFF6650a4)

/**
 * Purple-grey color for light theme (40% opacity).
 * Used as the secondary color in light mode.
 */
val PurpleGrey40 = Color(0xFF625b71)

/**
 * Pink color for light theme (40% opacity).
 * Used as the tertiary color in light mode.
 */
val Pink40 = Color(0xFF7D5260)